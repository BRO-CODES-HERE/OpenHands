{
  "name": "openhand",
  "version": "0.0.1",
  "private": true,
  "description": "Personal AI assistant — my own openclaw",
  "engines": {
    "node": ">=22.19.0"
  },
  "packageManager": "pnpm@11.5.0",
  "scripts": {
    "setup": "node scripts/setup.mjs",
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "@types/ws": "^8.18.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  },
  "dependencies": {
    "typebox": "^1.1.39",
    "ws": "^8.21.0"
  }
}
