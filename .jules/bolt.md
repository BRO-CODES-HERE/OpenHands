## 2023-10-27 - WebSocket Broadcast Performance with Strings vs Buffers
**Learning:** When broadcasting a message to multiple connected clients using the `ws` library, passing a `String` (e.g., from `JSON.stringify()`) to `ws.send(msg)` causes the library to internally allocate a new `Buffer` from that string for *every single client*. For a large number of connected clients, this creates massive unnecessary overhead.
**Action:** When broadcasting identical payloads to multiple clients, convert the string to a `Buffer` *once* (`const buf = Buffer.from(jsonStr);`), then loop and send that buffer using `ws.send(buf, { binary: false })`. The `{ binary: false }` flag ensures the frame is sent as text, matching the original string behavior, while achieving roughly ~2x faster broadcast times.

## 2024-06-25 - Node.js File System I/O Bottleneck on Sequential Reads
**Learning:** A common performance bottleneck in Node.js applications reading multiple small JSON files (e.g., loading a list of sessions/conversations from disk) is iterating using a sequential `for...of` loop with `await fs.readFile()`. Because Node is single-threaded, waiting for each file sequentially adds significant latency block on disk I/O, which scales linearly O(N) relative to the number of files.
**Action:** To read many independent small files, perform the reads concurrently. Map the array of file names to an array of promises returning the file read content, and `await Promise.all()`. This takes advantage of underlying asynchronous I/O and can reduce read times for directories with many files dramatically (e.g. up to 70% reduction in time for loading a list of sessions).
## 2024-11-20 - Memoizing File System Ensure Directory
**Learning:** Checking for directory existence or ensuring its creation before performing I/O operations (like `fs.mkdir(dir, { recursive: true })`) is a common pattern, but doing it on *every* single read or write operation introduces unnecessary file system bottleneck.
**Action:** Memoize the result of `ensureDir()` per instance (e.g. using a boolean flag `dirEnsured`). The flag starts out `false` and flips to `true` after the first successful `fs.mkdir`. Subsequent calls skip the I/O check, providing a simple yet safe optimization to file operations.
## 2024-11-21 - Unnecessary React Re-Renders for Long Lists
**Learning:** In React applications with a long list of items (like a chat history), mapping over an array with an inline component definition or complex DOM structure inside the main component body causes the entire list to re-render every time the parent component state changes (e.g., when the user types in an input field and updates state on every keystroke). This leads to significant UI lag.
**Action:** Extract the complex item logic into a separate functional component defined completely outside the main component body, and wrap it with `React.memo()`. This prevents the individual items from re-rendering unless their specific props change, greatly improving the performance of the parent component during frequent state updates like typing.
